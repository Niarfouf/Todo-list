export default function openProject() {
    const formDiv = document.querySelector("#popup-Project")
    const form = document.querySelector(".form-project")
    form.reset()
    formDiv.style.display = "block";
}